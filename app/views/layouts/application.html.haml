!!!
%html
  %head
    %title Museum of American G.I
    = stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
  %body
    %nav.navbar.navbar-default.navbar-fixed-top
      .container
        .navbar-header
          %button.navbar-toggle.collapsed{"aria-controls" => "navbar", "aria-expanded" => "false", "data-target" => "#navbar", "data-toggle" => "collapse", :type => "button"}
            %span.sr-only Toggle navigation
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          = link_to image_tag("logo-xs.gif", :alt => "Brand", :id => "logo"), homepage_index_path, :class => "navbar-brand"
        #navbar.navbar-collapse.collapse
          %ul.nav.navbar-nav
            %li.active= link_to "Home", homepage_index_path
            -#%li
            -#  %a{:href => "#about"} About
            %li= link_to "About", zipcodes_path
            %li
              %a{:href => "#contact"} Contact
            - if logged_in?
              -# TODO JavaScript not working
              -#%li= link_to "Admins", '#'
              -#%li.dropdown
              -#  %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
              -#    Account
              -#    %b.caret
              -#  %ul.dropdown-menu
              -#    %li= link_to "Profile", current_admin
              -#    %li= link_to "Settings", '#'
              -#    %li.divider
              -#    %li
              -#      = link_to "Log out", logout_path, method: "delete"
              -# %li= link_to "Profile", current_admin
              %li= link_to "Settings", edit_admin_path(current_admin)
              -# Customer does not want the add admin functionality. It is however still available on path /admins/new
              -# %li= link_to "Add Admin", signup_path
              %li= link_to "Log out", logout_path, method: "delete"
            - else
              -# Do not show log in link in the navigation bar.
              -# %li= link_to "Log in", login_path

    -# Show all alert messages from flash
    - flash.each do |message_type, message|
      %div{:class => "alert alert-#{message_type}"}= message

    = yield
    %footer.footer
      .container
        %p.text-muted The Dining Philosophers - Copyright (c) 2015.
        = link_to "Administration", login_path, :class => "navbar-brand"
        -#%a.navbar-brand{:href => "#admin_login"} Administration